# Basic environment variables

export EDITOR='nvim'                    # NOTE: install nvim (see tool list); vim is the fallback until then
export LANG='en_US.UTF-8'
export XDG_CONFIG_HOME="$HOME/.config"  # many tools (incl. git) honour this for their config location
