# zsh-syntax-highlighting — MUST be sourced last (it wraps all ZLE widgets),
# so this file is numbered 99 to load after everything else in conf.d.
source "$(brew --prefix)/share/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh"
